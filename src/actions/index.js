import * as departmentActions from './departments';
import * as employeeActions from './employees';
import * as fetchCountActions from './fetchCount';

const actions = {
  departmentActions,
  employeeActions,
  fetchCountActions
};

export default actions;
